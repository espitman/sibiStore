<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- The store server is intentionally plain HTTP on the user's home LAN. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
